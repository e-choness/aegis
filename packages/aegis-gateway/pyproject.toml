[project]
name = "aegis-gateway"
version = "2.0.0a0"
description = "Aegis v2 — umbrella package: CLI, server, core, and default policy packs."
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "aegis-cli",
    "aegis-core",
    "aegis-server",
    "aegis-pack-pii[pii]",
    "aegis-pack-llm-guard[llm-guard]",
    "aegis-pack-classification",
    "aegis-pack-residency",
    "aegis-pack-budgets",
]

[tool.uv.sources]
aegis-cli = { workspace = true }
aegis-core = { workspace = true }
aegis-server = { workspace = true }
aegis-pack-pii = { workspace = true }
aegis-pack-llm-guard = { workspace = true }
aegis-pack-classification = { workspace = true }
aegis-pack-residency = { workspace = true }
aegis-pack-budgets = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aegis_gateway"]
