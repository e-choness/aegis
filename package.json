{
  "name": "aegis-docs",
  "private": true,
  "type": "module",
  "description": "Documentation site for Aegis (VitePress). Python packages are managed by uv, not npm.",
  "scripts": {
    "docs:dev": "vitepress dev docs --host 0.0.0.0",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host 0.0.0.0"
  },
  "devDependencies": {
    "mermaid": "^12.0.0",
    "vitepress": "^1.6.4",
    "vitepress-plugin-mermaid": "^2.0.17"
  },
  "overrides": {
    "vite": "^7.3.6"
  }
}
